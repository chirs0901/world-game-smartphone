# ============================================================
# 场景规则 — 基准 2025
#
# 该场景下的额外规则，在加载 system.rules.yaml 之后叠加。
# 基准场景没有任何额外规则——所有规则来自系统默认。
# ============================================================

scenario: "base_2025"
description: "2025年基准场景，使用系统默认规则集，无额外覆盖。"

# 基准场景无额外规则
rules: []
